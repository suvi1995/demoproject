apiVersion: v1
kind: Service
metadata:
  name: {{ include "prj-chart.fullname" . }}
  labels:
    {{- include "prj-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prj-chart.selectorLabels" . | nindent 4 }}
